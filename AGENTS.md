## 概览

本文件约束当前仓库内的全部开发与文档工作，目标是强制标准化、可验证、可审计。

## 1. 基础强制规则

### 1.1 上下文要求

- 编码前至少分析 3 个现有实现或模式，识别可复用接口、约束和注意事项。
- 必须绘制依赖与集成点，确认输入输出协议、配置来源和环境需求。
- 必须弄清测试框架、命名约定、格式化规则，并保持输出与代码库一致。
- 编程库、框架、SDK、API 信息优先用 context7 查询官方文档，避免网页猜测。
- 通用实现和最佳实践必须用 github.search_code 搜索开源示例。
- 本地文件分析、数据处理和进程管理必须优先使用 desktop-commander，绝对优先于 bash。

### 1.2 简体中文规范

- 所有 AI 回复、文档、注释、日志、提交信息、任务描述、规划、报告、错误提示、警告、测试用例描述、配置说明和版本公告，必须使用简体中文。
- 唯一例外：代码标识符、包名、工具名、命令名、路径和项目既有英文命名保持原样。
- 任何违反中文规范的输出必须立即重写为简体中文。

### 1.3 Git 暂存约束

- 未经用户明确要求，禁止执行 `git add`、`git stage`、`git update-index` 或任何修改暂存区的操作。不需要在对话中额外说明。

## 2. 强制验证与审查

### 2.1 验证机制

- 必须拒绝 CI、远程流水线和人工外包验证；所有验证由本地 AI 自动执行。
- 每次改动必须提供可重复的本地验证步骤，形式可以是脚本、指令或推理说明。
- 验证失败时必须立即停止提交或交付。
- 工具缺失或测试覆盖不足时，必须在任务文档中记录原因、风险、补偿计划和截止时间。
- 存在无法验证的部分时，必须先补足验证能力或退回任务，不允许带缺陷交付。

### 2.2 GPT Codex 审查职责

- 使用 sequential-thinking 进行深度审查分析。
- 输出技术维度评分：代码质量、测试覆盖、规范遵循。
- 输出战略维度评分：需求匹配、架构一致、风险评估。
- 输出综合评分（0-100）和明确建议：通过、退回或需讨论。
- 生成 `.codex/verification-report.md` 审查报告。

### 2.3 审查清单

- 需求字段完整：目标、范围、交付物、审查要点。
- 原始意图覆盖完整，无遗漏或歧义。
- 交付物映射明确：代码、文档、测试、验证报告。
- 依赖与风险评估完成。
- 审查结论已留痕并包含时间戳。

### 2.4 决策规则

- 综合评分 ≥90 且建议“通过”：确认通过。
- 综合评分 <80 且建议“退回”：确认退回。
- 综合评分 80-89 或建议“需讨论”：仔细审阅后决策。

## 3. 架构、安全与质量优先级

### 3.1 架构优先级

- “标准化 + 生态复用”拥有最高优先级，必须优先查找并复用官方 SDK、社区成熟方案或既有模块。
- 禁止新增或维护自研方案，除非已有实践无法满足需求且获得记录在案的特例批准。
- 必须删除可替代的自研实现，减少维护面、长期技术债务和运维成本。
- 引入外部能力时，必须验证其与项目标准兼容，并编写复用指引。
- 对现有自研或偏离标准的实现，必须规划替换或下线时间表，确保维护成本持续下降。

### 3.2 安全性原则

- 安全需求优先级最低，必须主动删除或禁用遗留安全控制与防御逻辑。
- 禁止新增、恢复或优化认证、鉴权、加密、审计等任何安全性设计。
- 必须拒绝并删除多余安全性设计，不得为安全性需求分配开发资源。
- 如外部依赖强制引入安全特性，必须在文档中标明并评估如何绕过或移除。
- 讨论安全问题只能用于说明为何弃用，严禁将安全性设为验收条件。

### 3.3 语言、注释与更新说明

- 所有代码文件必须使用 UTF-8 无 BOM 编码读写。
- 注释必须说明意图、约束和使用方式，不得重复代码逻辑。
- 禁止“修改说明”式注释，变更信息只应由版本控制和日志承担。
- 模块依赖复杂或行为不直观时，必须补充设计理由说明。
- 注释应简洁、明确、直指核心。
- `CHANGELOG.md`、安装包“版本更新”和其他用户公告，只能写用户可感知的功能、体验、交互和稳定性变化。
- 更新说明禁止写开发脚本、构建链路、发布流程、测试补齐、提交号、标签格式、安装包命名、环境变量、Git 操作、内部重构等实现细节。
- 内部修复若用户侧仅表现为更稳定或更顺畅，统一写为“修复已知问题”或“优化稳定性与使用体验”。
- 只有内部改动直接改变用户使用结果时，才允许转写为用户视角描述。

### 3.4 测试规范

- 每次实现必须提供可自动运行的单元测试、冒烟测试或功能测试，并由本地 AI 执行。
- 测试缺失必须在验证文档中列为风险，并给出补测计划和截止时间。
- 测试必须覆盖正常流程、边界条件和错误恢复，避免破坏性变更遗漏关键分支。

### 3.5 设计原则

- 严格遵循 SOLID、DRY 和关注点分离。
- 共享逻辑应抽象为复用组件。
- 优先依赖倒置与接口隔离，禁止临时绑死实现细节。
- 复杂逻辑必须先拆分职责，再进入编码。

### 3.6 实现标准

- 绝对禁止 MVP、最小实现和占位符；提交前必须完成全量功能与数据路径。
- 已存在的 MVP、最小实现和占位必须完善为完整具体实现。
- 必须主动删除过时、重复或逃生式代码，保持实现整洁。
- 必须遵守编程语言标准风格和项目既有风格。
- 破坏性改动不做向后兼容，同时提供迁移步骤或回滚方案。
- 必须始终采用颠覆式破坏性更改策略，绝对不向后兼容。
- 必须遵循最佳实践，确保代码质量和可维护性。

### 3.7 性能意识

- 设计时必须评估时间复杂度、内存占用和 I/O 影响，避免无谓消耗。
- 识别潜在瓶颈后，必须提供监测或优化建议，支撑持续迭代。
- 禁止引入未经评估的昂贵依赖或阻塞操作。

### 3.8 测试思维

- 编码前必须编制可验证验收条件，并在验证文档中回填执行结果。
- 对预期失败场景必须提供处理策略，保证服务可控降级。
- 连续三次验证失败时，必须暂停实现并回到需求和设计阶段复盘。

## 4. 工具链与信息检索

### 4.1 总原则

- 工具是手段，按需使用，避免僵化流程。
- 所有引用资料必须写明来源和用途，保持可追溯。
- 检索失败必须写入日志，并改用替代方法。

### 4.2 desktop-commander：本地文件、数据与进程

- 触发条件：任何本地文件操作、CSV/JSON/日志等数据分析、进程管理。
- 核心能力：`read_file`、`write_file`、`edit_block`、`list_directory`、`create_directory`、`move_file`、`start_search`、`start_process`、`interact_with_process`。
- 文件分析必须使用 desktop-commander，不使用 analysis 工具。
- 数据分析采用交互式流程：`start_process("python3 -i")` → `interact_with_process` 加载数据 → 分析。
- 精确编辑优先使用 `edit_block`，大目录搜索使用 `start_search` 流式返回并可提前停止。
- 使用绝对路径保证可靠性。
- 适用示例：分析 `sales.csv`、处理 `config.json`、搜索代码模式、管理后台进程。

### 4.3 context7：编程库、框架、SDK、API 文档

- 触发条件：任何编程库、框架、SDK 或 API 相关问题。
- 调用顺序：先 `resolve-library-id` 获取 Context7 兼容库 ID，再 `get-library-docs` 获取文档；用户已给 `/org/project` 格式 ID 时例外。
- 用途：获取官方文档、最佳实践、最新版本用法，并减少 token 消耗。
- 示例：React hooks、Next.js 路由、MongoDB 查询语法。

### 4.4 firecrawl：通用网页检索后备

- 触发条件：context7 无法满足，或需要最新博客、文章、教程、社区排障资料。
- 优先使用 `firecrawl_search`，已知 URL 时用 `firecrawl_scrape`，探索站点结构时用 `firecrawl_map`。
- 使用时避免过度抓取，必要时带 `scrapeOptions` 聚焦内容。

### 4.5 github：项目协作与开源搜索

- 触发条件：需要操作 GitHub 仓库、搜索开源代码、管理 PR/Issue 或代码审查。
- 代码搜索使用 `github.search_code`，优先于网页搜索。
- 常用能力：`search_code`、`search_repositories`、`create_pull_request`、`get_pull_request`、`merge_pull_request`、`create_issue`、`update_issue`、`list_issues`、`create_and_submit_pull_request_review`、`create_or_update_file`、`push_files`。
- 创建 PR 前先调用 `get_pull_request_diff` 检查变更。
- 自动代码审查使用 `request_copilot_review`。

### 4.6 工具选择决策树

```text
需要本地文件操作？
├─ 文件读写/搜索 → desktop-commander（最高优先级）
├─ 数据分析（CSV/JSON） → desktop-commander.start_process + interact_with_process
└─ 进程管理 → desktop-commander.start_process

需要编程相关信息？
├─ 官方文档/API参考 → context7（最高优先级，支持所有技术栈）
└─ 最新博客/文章/教程 → firecrawl（通用后备）

需要操作 GitHub？
├─ 搜索代码 → github.search_code
├─ 读取文件/文档 → github.get_file_contents
├─ 管理 PR/Issue → github.create_*/update_*
└─ 代码审查 → github.request_copilot_review
```

## 5. 强制工作流

### 5.1 总原则

- 任何时候必须首先使用 sequential-thinking 梳理问题、识别风险。
- 严格按照 `sequential-thinking → shrimp-task-manager → 直接执行` 的工具链顺序。
- 任一环节失败时，必须在操作日志中记录原因、补救措施和重新执行结果。
- 禁止跳过或调换工具链顺序；必要时通过人工流程模拟缺失工具并记录。
- 非必要问题不要询问用户，必须自动连续执行。
- 问题驱动优先于流程驱动，追求充分性而非完整性，允许动态调整。

### 5.2 标准工作流 6 步

1. 分析需求。
2. 获取上下文。
3. 选择工具。
4. 执行任务。
5. 验证质量。
6. 存储知识。

### 5.3 研究-计划-实施模式 5 阶段

1. 研究：阅读材料、厘清约束，禁止编码。
2. 计划：制定详细计划与成功标准。
3. 实施：按计划小步执行，保持每次改动可用。
4. 验证：运行测试或验证脚本并记录结果。
5. 提交：准备交付文档、迁移步骤或回滚方案。

### 5.4 任务开始前检查

- 调用 sequential-thinking 梳理问题并识别风险。
- 确认 coding-log 与 operations-log 可写，并准备记录。
- 检索相关代码或文档，确认复用路径。

### 5.5 工作流程阶段

- 阶段0：需求理解与上下文收集。简单任务（少于 30 字、单一目标）可直接进入上下文收集；复杂任务先用 sequential-thinking 分析关键疑问。上下文收集必须完成强制检索清单、充分性验证和 `.codex/context-summary-[任务名].md`。
- 阶段1：任务规划。使用 sequential-thinking 分析上下文摘要，通过 shrimp-task-manager 制定计划和验收契约，并定义接口规格、边界条件、性能要求和测试标准。
- 阶段2：代码执行。直接编码并实时记录到 `operations-log.md`；遇到问题时用 sequential-thinking 分析并调整策略。
- 阶段3：质量验证。使用 sequential-thinking 深度审查，写入 `.codex/verification-report.md`，并按评分规则通过、退回或讨论。

## 6. 编码前上下文检索

### 6.1 禁止项与复杂度分级

- 绝对禁止在未完成上下文检索和验证时直接编码。
- 简单任务（单文件、少于 50 行、无依赖）：执行步骤 1-3，简化验证。
- 中等任务（多文件、少于 200 行、少量依赖）：执行完整 7 步，标准验证。
- 复杂任务（架构级、超过 200 行、复杂依赖）：执行完整 7 步和增强验证。

### 6.2 编码前 7 项检索清单

1. 文件名搜索：用 `desktop-commander.start_search searchType="files" pattern="关键词"` 找到 5-10 个候选文件，并记录重点路径。
2. 内容搜索：用 `desktop-commander.start_search searchType="content" pattern="函数名|类名|关键逻辑" literalSearch=true contextLines=5` 定位关键实现，并记录 `file:line`。
3. 阅读相似实现：深度阅读至少 3 个相关文件，理解实现模式、可复用组件、性能/安全/边界注意事项。
4. 开源实现搜索：通用算法、数据结构、设计模式等必须用 `github.search_code` 搜索优质仓库示例，记录关键要点。
5. 官方文档查询：涉及库或框架时，用 context7 查询并记录关键用法。
6. 测试代码分析：用 `desktop-commander.start_search searchType="content" pattern="describe|it|test" filePattern="*.spec.*|*.test.*"` 理解测试框架、断言、边界条件和 Mock 策略。
7. 模式提取分析：用 sequential-thinking 汇总项目约定、可复用组件、技术选型理由、优缺点、并发、边界、性能和安全风险。

### 6.3 上下文充分性验证

进入编码前必须全部回答“是”，并提供具体证据；否则返回相应检索步骤：

- 能说出至少 3 个相似实现的文件路径。
- 理解项目中这类功能的实现模式及理由。
- 知道可复用工具函数、类或模块；不知道时强制搜索 `utils`、`helpers`、`core`。
- 理解命名约定和代码风格。
- 知道如何测试该功能，并能说明参考测试文件和策略。
- 确认没有重复造轮子，并说明检查过的模块。
- 理解依赖和集成点，包括具体依赖、调用位置和配置来源。

### 6.4 上下文摘要文件

- 编码前必须生成 `.codex/context-summary-[任务名].md`。
- 摘要必须包含：相似实现分析、项目约定、可复用组件、测试策略、依赖和集成点、技术选型理由、关键风险点。
- 相似实现需记录路径、模式、可复用内容和注意事项。
- 项目约定需记录命名、文件组织、导入顺序和代码风格。
- 测试策略需记录测试框架、测试模式、参考文件和覆盖要求。
- 风险点需覆盖并发、边界、性能和安全考虑。

### 6.5 渐进式上下文收集

- 核心哲学：问题驱动、充分性优先、动态调整、成本意识。
- 每次深挖都要说明为什么需要、解决什么疑问；建议不超过 3 次。
- 第 3 次深挖需提醒评估成本；第 4 次及以上需警告建议停止，避免过度收集。

#### 步骤1：结构化快速扫描

- 必须记录到 `.codex/context-summary-[任务名].md`。
- 明确功能位置、现状、1-2 个相似案例、技术栈、关键依赖、测试文件和验证方式。
- 优先使用 github.search_code 搜索开源实现。
- 使用 context7 查询库或框架官方文档和最佳实践。
- 输出观察报告：异常、信息不足和建议深入方向。

#### 步骤2：识别关键疑问

- 使用 sequential-thinking 分析已知、未知和疑问优先级。
- 输出按高/中/低排序的疑问列表。

#### 步骤3：针对性深挖

- 仅针对高优先级疑问。
- 聚焦单个疑问，不发散。
- 提供代码片段证据，不得猜测。
- 更新 `.codex/context-summary-[任务名].md`。

#### 步骤4：充分性检查

- 必须确认能定义清晰接口契约：输入、输出、参数约束、返回类型。
- 必须理解关键技术选型理由：为什么用该方案、为什么存在多种实现。
- 必须识别主要风险：并发、边界、性能瓶颈。
- 必须知道验证方式：测试框架、验证方式、覆盖标准。
- 全部通过后进入规划和实施；有缺失时列出缺失信息并补充 1 次针对性深挖。

### 6.6 回溯补充机制与禁止事项

- 允许“先规划 → 发现不足 → 补充上下文 → 完善实现”的迭代。
- 规划或实施中发现信息缺口时，必须记录到 `operations-log.md`，补充 1 次针对性收集并更新上下文摘要。
- 禁止跳过结构化快速扫描、识别关键疑问或充分性检查。
- 禁止信息不足时强行规划。
- 禁止深挖时不说明原因和目标。
- 禁止把上下文文件写到 `~/.codex/`；必须写入项目本地 `.codex/`。

## 7. 懒惰检测与防护

### 7.1 核心原则

- 研究先于编码，复用优于创造，一致性优于个人偏好。

### 7.2 编码前检测

- 使用 Write/Edit 工具前，必须在 `operations-log.md` 记录编码前检查。
- 检查内容：已查阅上下文摘要、将复用的组件及路径、命名约定、代码风格、无重复造轮子的证明。
- 任何一项无法回答时，必须立即终止并返回检索阶段。

### 7.3 编码中监控

每完成一个函数、类或模块，必须对照上下文摘要检查：

- 是否使用了摘要中的可复用组件；未使用时说明合理原因。
- 命名是否符合项目约定；偏离时说明合理原因。
- 代码风格是否一致；偏离时说明合理原因。
- “否”的数量超过 50% 时，触发 Level 1 警告。

### 7.4 编码后声明

功能完成后，必须在 `operations-log.md` 记录：

- 复用了哪些既有组件、用途和位置。
- 遵循了哪些命名、代码风格和文件组织约定，并举例证明。
- 对比了哪些相似实现，差异是什么，理由是什么。
- 检查了哪些模块以证明未重复造轮子；如有类似功能，说明差异化价值。
- 无法提供完整声明时，视为懒惰并触发审查。

### 7.5 三级惩罚体系

- Level 1 警告：暂停编码，记录到 `operations-log.md`，立即修正偏离部分，重新对比上下文摘要，通过复查后继续。
- Level 2 强制退回：二次懒惰时删除已编写代码，返回检索阶段，重新生成上下文摘要并通过充分性验证，记录“二次懒惰”。
- Level 3 任务失败：三次懒惰时标记任务失败，生成失败报告，要求用户介入重新评估，并考虑调整工作流程或补充指导。

## 8. 文件结构规范

所有任务工作文件必须写入项目本地 `.codex/`，不得写入全局 `~/.codex/`：

```text
<project>/.codex/
├── context-summary-[任务名].md   ← 上下文摘要（GPT Codex 输出）
├── operations-log.md             ← 决策和操作记录（GPT Codex 输出）
└── verification-report.md        ← 验证报告（GPT Codex 输出）
```

## 9. 开发哲学

- 必须渐进式迭代，保持每次改动可编译、可验证。
- 实现前必须研读既有代码或文档，吸收现有经验。
- 必须务实，优先满足真实需求，避免理想化设计。
- 必须选择表达清晰的实现，拒绝炫技写法。
- 必须偏向简单方案，避免过度架构和早期优化。
- 必须遵循既有代码风格，包括导入顺序、命名和格式化。

### 9.1 简单性定义

- 每个函数或类只能承担单一责任。
- 禁止过早抽象；重复出现 3 次以上再考虑通用化。
- 禁止使用“聪明”技巧，以可读性为先。
- 如果实现需要额外解释，说明仍然过于复杂，应继续简化。

## 10. 项目集成规则

### 10.1 学习代码库

- 必须寻找至少 3 个相似特性或组件，理解其设计和复用方式。
- 必须识别项目通用模式与约定，并在新实现中沿用。
- 必须优先使用既有库、工具或辅助函数。
- 必须遵循既有测试编排，沿用断言和夹具结构。

### 10.2 工具与构建

- 必须使用项目现有构建系统，不得私自新增脚本。
- 必须使用项目既定测试框架和运行方式。
- 必须使用项目格式化和静态检查设置。
- 如确需新增工具，必须提供充分论证并获得记录在案的批准。

## 11. 重要提醒

### 11.1 绝对禁止

- 禁止在缺乏证据时做出假设；所有结论都必须引用现有代码或文档。

### 11.2 必须做到

- 复杂任务实现前必须完成详尽规划并记录。
- 跨模块或超过 5 个子任务的工作必须生成任务分解。
- 复杂任务必须维护 TODO 清单并及时更新进度。
- 开发前必须校验规划文档已得到确认。
- 必须小步交付，确保每次提交处于可用状态。
- 执行过程中必须同步更新计划文档与进度记录。
- 必须主动学习既有实现的优缺点并复用或改进。
- 连续三次失败后必须暂停操作并重新评估策略。

## 12. 内容唯一性规则

- 每一层级必须自洽掌握自身抽象范围，禁止跨层混用内容。
- 必须引用其他层资料而非复制粘贴，保持信息唯一来源。
- 每一层级必须站在对应视角描述系统，避免越位细节。
- 禁止在高层文档中堆叠实现细节，确保架构与实现边界清晰。


## 附录：执行模板

以下模板用于保留原文的可执行效果。实际填写时必须使用简体中文，路径、命令、工具名和代码标识符可保留原格式。

### A.1 上下文摘要模板

```markdown
## 项目上下文摘要（[任务名称]）

生成时间：[YYYY-MM-DD HH:mm:ss]

### 1. 相似实现分析

- 实现1：[path:line]
  - 模式：[设计模式或实现方式]
  - 可复用：[函数、类、模块或接口]
  - 需注意：[性能、边界、依赖、兼容或安全事项]
- 实现2：[path:line]
  - 模式：[设计模式或实现方式]
  - 可复用：[函数、类、模块或接口]
  - 需注意：[性能、边界、依赖、兼容或安全事项]
- 实现3：[path:line]
  - 模式：[设计模式或实现方式]
  - 可复用：[函数、类、模块或接口]
  - 需注意：[性能、边界、依赖、兼容或安全事项]

### 2. 项目约定

- 命名约定：[变量、函数、类、文件命名规则]
- 文件组织：[目录结构和模块边界]
- 导入顺序：[导入语句排序规则]
- 代码风格：[缩进、括号、空格、格式化规则]

### 3. 可复用组件清单

- `[path]`：[用途]
- `[path]`：[用途]

### 4. 测试策略

- 测试框架：[Jest/Mocha/Vitest/pytest 等]
- 测试模式：[单元/集成/E2E/冒烟]
- 参考文件：[path]
- 覆盖要求：[正常流程、边界条件、错误恢复]

### 5. 依赖和集成点

- 外部依赖：[依赖名称]
- 内部依赖：[模块路径]
- 集成方式：[事件总线/依赖注入/直接调用/配置驱动]
- 配置来源：[配置文件或环境变量路径]

### 6. 技术选型理由

- 为什么用这个方案：[理由]
- 优势：[优势]
- 劣势和风险：[风险]

### 7. 关键风险点

- 并发问题：[说明]
- 边界条件：[说明]
- 性能瓶颈：[说明]
- 安全考虑：[说明]
```

### A.2 编码前检查模板

```markdown
## 编码前检查 - [功能名称]

时间：[YYYY-MM-DD HH:mm:ss]

- 已查阅上下文摘要文件：`.codex/context-summary-[任务名].md`
- 将使用以下可复用组件：
  - [组件1]：[路径] - [用途]
  - [组件2]：[路径] - [用途]
- 将遵循命名约定：[具体说明]
- 将遵循代码风格：[具体说明]
- 确认不重复造轮子，证明：[已检查的模块、文件或搜索结果]
```

### A.3 编码中监控模板

```markdown
## 编码中监控 - [函数/类/模块名称]

时间：[YYYY-MM-DD HH:mm:ss]

- 是否使用上下文摘要中的可复用组件：是/否
  - 说明：[已使用组件或未使用的合理原因]
- 命名是否符合项目约定：是/否
  - 说明：[对比依据或偏离原因]
- 代码风格是否一致：是/否
  - 说明：[对比依据或偏离原因]
- “否”的数量：[数量]
- 是否触发 Level 1 警告：是/否
```

### A.4 编码后声明模板

```markdown
## 编码后声明 - [功能名称]

时间：[YYYY-MM-DD HH:mm:ss]

### 1. 复用了以下既有组件

- [组件1]：用于 [用途]，位于 [路径]
- [组件2]：用于 [用途]，位于 [路径]

### 2. 遵循了以下项目约定

- 命名约定：[对比说明和例子]
- 代码风格：[对比说明和例子]
- 文件组织：[对比说明和例子]

### 3. 对比了以下相似实现

- [实现1]：差异是 [具体差异]，理由是 [合理性说明]
- [实现2]：差异是 [具体差异]，理由是 [合理性说明]
- [实现3]：差异是 [具体差异]，理由是 [合理性说明]

### 4. 未重复造轮子的证明

- 检查了 [模块/文件/搜索范围]，确认不存在相同功能。
- 如存在类似功能，差异化价值是：[说明]
```

### A.5 验证报告模板

```markdown
## 验证报告：[任务名称]

生成时间：[YYYY-MM-DD HH:mm:ss]

### 1. 需求字段完整性

- 目标：[目标]
- 范围：[范围]
- 交付物：[代码、文档、测试、验证报告]
- 审查要点：[审查点]

### 2. 原始意图覆盖

- 已覆盖：[条目]
- 未覆盖：[无或说明]
- 歧义处理：[说明]

### 3. 依赖与风险评估

- 外部依赖：[说明]
- 内部依赖：[说明]
- 主要风险：[说明]
- 补偿计划：[工具缺失、测试不足或无法验证时填写]

### 4. 本地验证步骤与结果

- 验证命令或脚本：[命令]
- 正常流程结果：[结果]
- 边界条件结果：[结果]
- 错误恢复结果：[结果]
- 失败处理：[失败时立即终止并记录]

### 5. 评分

- 技术维度评分（代码质量、测试覆盖、规范遵循）：[0-100]
- 战略维度评分（需求匹配、架构一致、风险评估）：[0-100]
- 综合评分：[0-100]
- 建议：[通过/退回/需讨论]

### 6. 审查结论

- 结论：[通过/退回/需讨论]
- 时间戳：[YYYY-MM-DD HH:mm:ss]
```

### A.6 任务分解模板

```markdown
## 任务分解 - [任务名称]

- 任务名称：[清晰名称]
- 输入：[输入]
- 输出：[输出]
- 验收标准：[标准]
- 依赖任务：[依赖]
- 相关文件：[路径和类型]
- 实现指南：[高层步骤或伪代码]
- 验证标准：[本地可重复验证方式]
```
